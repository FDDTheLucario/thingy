export default class SRDItem {
    name: string;
    index: string;
    url: string;

    constructor(name: string, index: string, url: string) {
        this.name = name;
        this.index = index;
        this.url = url;
    }
} 
